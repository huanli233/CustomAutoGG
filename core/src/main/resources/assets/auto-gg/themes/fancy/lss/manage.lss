@import super();

.input-container {
  border-radius: 10;
  background-color: rgba(50, 50, 50, 100);
  filter: blur(5);

  .label-name {
    margin-left: 23;
  }
}

.remove-container {
  border-radius: 10;
  background-color: rgba(50, 50, 50, 100);
  filter: blur(5);
}